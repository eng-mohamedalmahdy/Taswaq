package com.example.taswaq.domain.model

data class DomainAddress(
                    var cityAddress: String,
                    var streetAddress: String,
                    var latAddress: Double,
                    var langAddress: Double,
                    var notes: String   )